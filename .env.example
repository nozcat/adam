# Application Mode
# Supported modes: adam (default), eve
MODE=adam

# Linear API Configuration
# Create an API key at https://linear.app/settings/api
LINEAR_API_KEY=

# Agent-specific Linear API keys (optional)
# These will override LINEAR_API_KEY for specific agents
LINEAR_API_KEY_ADAM=
LINEAR_API_KEY_EVE=

# GitHub Configuration
# Create a personal access token at https://github.com/settings/tokens
GITHUB_TOKEN=
GITHUB_USERNAME=
GITHUB_EMAIL=

# Agent-specific GitHub configuration (optional)
# These will override the base GitHub settings for specific agents
GITHUB_TOKEN_ADAM=
GITHUB_TOKEN_EVE=
GITHUB_USERNAME_ADAM=
GITHUB_USERNAME_EVE=
GITHUB_EMAIL_ADAM=
GITHUB_EMAIL_EVE=

# Base branch for pull requests
BASE_BRANCH=main

# Whether to print debug logs
DEBUG=false

# Poll interval in seconds for checking Linear issues and GitHub PR feedback
# Default: 30 seconds
POLL_INTERVAL=30

# Directory where git repositories are stored
# Default: ./repos
REPOS_DIR=./repos

# Comma-separated list of repositories to clone
# Format: <owner>/<repo-name>,<owner2>/<repo-name2>,...
# Example: facebook/react,vuejs/vue,angular/angular
REPOS=

# API server configuration
# If API_SERVER is set, Adam and Eve will connect to the specified external API server
# If API_SERVER is not set (null/empty), Adam and Eve will start their own local API server
# Format: Can be a full URL (http://localhost:8880) or just host:port (localhost:8880)
# Default: null (starts local API server)
API_SERVER=

# API server port (used when starting local API server)
# Default: 8880
API_PORT=8880